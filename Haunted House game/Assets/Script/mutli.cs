using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 多用途对象脚本
/// 此脚本作为替代方案，解决Unity场景中丢失脚本的引用错误
/// </summary>
public class mutli : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}